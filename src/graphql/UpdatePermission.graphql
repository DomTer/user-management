mutation CreatePermission($id: ID!, $name: String!, $description: String) {
    updatePermission(
        input: {
            id: $id
            name: $name
            description: $description
        }
    ) {
        id
    }
}
}
